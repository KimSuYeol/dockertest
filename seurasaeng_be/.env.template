# Seurasaeng Backend Environment Variables Template
# 프로덕션 환경에서 사용할 환경변수들을 설정합니다.

# Spring 프로파일 설정
SPRING_PROFILES_ACTIVE=prod

# 데이터베이스 설정 (PostgreSQL)
DB_URL=jdbc:postgresql://postgres:5432/seuraseung?currentSchema=seurasaeng-prod
DB_USERNAME=seuraseung
DB_PASSWORD=seuraseung123!

# Redis 설정
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123!
REDIS_DB=0

# JWT 보안 설정
JWT_KEY=seuraseung-jwt-secret-key-2024-prod-version-very-long-secret

# QR 코드 암호화 키
ENCRYPTION_KEY=seuraseung-encryption-key-2024

# CORS 설정 (프론트엔드 도메인)
CORS_ALLOWED_ORIGINS=https://seurasaeng.site,http://13.125.200.221

# AWS S3 설정 (실제 운영시 GitHub Secrets에서 주입)
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET_KEY=your-aws-secret-key
AWS_REGION=ap-northeast-2
AWS_BUCKET=seuraseung-bucket

# 이메일 설정 (Gmail SMTP)
MAIL_USERNAME=your-gmail@gmail.com
MAIL_PASSWORD=your-app-password

# ===========================================
# GitHub Secrets에서 설정해야 할 민감한 정보들
# ===========================================

# 실제 운영환경에서는 다음 환경변수들을 GitHub Secrets로 관리하세요:
# - AWS_ACCESS_KEY
# - AWS_SECRET_KEY  
# - MAIL_USERNAME
# - MAIL_PASSWORD
# - JWT_KEY (더 복잡한 키 사용)
# - ENCRYPTION_KEY (더 복잡한 키 사용)

# ===========================================
# 개발 환경용 설정 (.env.dev)
# ===========================================

# 개발 환경에서는 다음과 같이 설정하세요:
# SPRING_PROFILES_ACTIVE=dev
# DB_URL=jdbc:postgresql://localhost:5432/seuraseung?currentSchema=seurasaeng-test
# REDIS_DB=1
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080